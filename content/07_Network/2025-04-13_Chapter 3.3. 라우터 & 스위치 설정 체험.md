---
aliases: 
tags: 
created_at: 2025 04 13 11:48:30
updated_at: 2025 04 13 11:49:00
---

# 📚 Chapter 3.3: 라우터 & 스위치 설정 체험

---

## ✅ 왜 체험이 필요한가?

- 우리가 지금까지 배운 **IP, MAC, 포트, 패킷**  
    → 전부! 라우터랑 스위치가 실제로 **처리하는 일**이다!

이제 그걸 직접 **CLI 명령어든, GUI든, 시뮬레이터든** 체험해보는 게 목표다!  
네트워크가 **어떻게 연결되고 흐르는지** 손에 감 잡아보자!

---

## 🧱 라우터 vs 스위치 역할 복습

|구분|스위치|라우터|
|---|---|---|
|계층|OSI 2계층 (MAC 기반)|OSI 3계층 (IP 기반)|
|기능|LAN 내 장비 연결|서로 다른 네트워크 연결|
|주소 기반|MAC 주소|IP 주소|
|대표 장비|허브보다 똑똑함|공유기, L3 라우터|

---

## 🛠️ 시뮬레이터 추천

|도구|특징|
|---|---|
|**Cisco Packet Tracer**|Cisco 공식 무료 시뮬레이터, 추천!!|
|GNS3|전문가용, 복잡한 네트워크 가능|
|EVE-NG|라우터/방화벽/서버까지 다 지원|
|VMWare + Ubuntu|리눅스로 간단한 라우팅 실습 가능|

---

## ✅ Packet Tracer로 라우터 설정 실습 예시

1. 라우터 하나, PC 두 대 연결
    
2. CLI 열고 다음 입력!

`Router> enable Router# configure terminal Router(config)# interface gig0/0 Router(config-if)# ip address 192.168.1.1 255.255.255.0 Router(config-if)# no shutdown Router(config-if)# exit Router(config)# interface gig0/1 Router(config-if)# ip address 192.168.2.1 255.255.255.0 Router(config-if)# no shutdown Router(config-if)# exit`

→ 이러면 라우터가 두 개의 네트워크를 연결할 준비 끝!

---

## ✅ 스위치 VLAN 간단 실습

`Switch> enable Switch# configure terminal Switch(config)# vlan 10 Switch(config-vlan)# name PC_Group_A Switch(config-vlan)# exit  Switch(config)# interface fastethernet0/1 Switch(config-if)# switchport access vlan 10 Switch(config-if)# exit`

→ VLAN 만들어서 포트 나눠주는 기본 구조다!

---

## 💡 실습 꿀팁

- VLAN은 "스위치 안에서 네트워크를 논리적으로 나누는 방법"
    
- 라우터는 "서로 다른 네트워크(서브넷)를 이어주는 다리"
    
- CLI 익숙해지면 실무에서도 **방화벽, VPN, NAT 장비 설정**에도 엄청 도움됨

---

## 🧠 실습 없이 감각 잡고 싶다면?

Packet Tracer 열고 이렇게 연결만 해봐도 좋아:

1. PC 1 → 스위치 → 라우터 → 스위치 → PC 2
    
2. 각 장비에 IP 세팅
    
3. `ping` 명령어로 통신 확인

> Ping 통과하면?  
> 라우팅/스위칭 성공! 🎉

---

## ✅ 정리 요약

|장비|역할|실습 포인트|
|---|---|---|
|라우터|다른 네트워크 연결|인터페이스별 IP 설정|
|스위치|LAN 내부 장비 연결|VLAN 설정, 포트 매핑|
|실습 도구|Packet Tracer, GNS3|CLI 명령어 연습, 구조 설계|

---

## 다음 수업 예고 🎓

📦 **Chapter 3.4: 방화벽 개념과 기본 규칙 설정**

> "어떤 포트 열고, 어떤 IP 막고"  
> **보안의 제1선! 인바운드/아웃바운드 규칙 확실히 이해하자!**
